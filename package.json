{
  "name": "personal-website",
  "version": "0.0.1",
  "scripts": {
    "dev": "svelte-kit dev",
    "build": "svelte-kit build",
    "start": "node build",
    "preview": "svelte-kit preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-check --tsconfig ./tsconfig.json --watch",
    "logs": "heroku logs --tail",
    "logs:api": "heroku logs -t -d api",
    "logs:router": "heroku logs -t -d router",
    "logs:app": "heroku logs -t -s app"
  },
  "devDependencies": {
    "@sveltejs/adapter-node": "next",
    "@sveltejs/kit": "next",
    "svelte": "^3.46.4",
    "svelte-check": "^2.4.6",
    "svelte-preprocess": "^4.10.4",
    "tslib": "^2.3.1",
    "typescript": "~4.5.5"
  },
  "engines": {
    "node": ">=17.7.2"
  },
  "type": "module"
}