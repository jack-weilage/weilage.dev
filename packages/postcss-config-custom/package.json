{
	"private": true,
	"name": "postcss-config-custom",
	"version": "0.0.1",
	"description": "A custom PostCSS config for weilage.dev",
	"main": "index.js",
	"license": "ISC",
	"scripts": {
		"lint:eslint": "eslint --cache . --ignore-path ../../.prettierignore",
		"lint:prettier": "prettier --cache --check . --ignore-path=../../.prettierignore",
		"lint": "concurrently pnpm:lint:* --kill-others-on-fail",
		"format": "prettier --cache --write . --ignore-path=../../.prettierignore"
	},
	"dependencies": {
		"autoprefixer": "^10.4.14",
		"postcss": "^8.4.23",
		"postcss-preset-env": "^8.3.2"
	}
}
